import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["res.cloudinary.com", "lamia.com.vn", "localhost", "tse3.mm.bing.net", "tse1.mm.bing.net"],
  },
};

export default nextConfig;
